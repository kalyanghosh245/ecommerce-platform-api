first install nest globally
npm install -g @nestjs/cli

cd ../services
nest new product-service --strict
cd product-service

npm install @nestjs/typeorm typeorm pg @nestjs/config @nestjs/swagger class-validator class-transformer
npm install opossum # Circuit breaker for inter-service calls
npm install ../../shared  # Link shared library